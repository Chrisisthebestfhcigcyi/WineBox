clear
termux-change-repo
termux-setup-storage
pkg install git
sleep 2
echo "installing wget
pkg install wget -y
sleep 2
echo wget succefully installed!!!
sleep 2
echo subscribe to littlebodybigfan188 for updates
clear
pkg install termux-am -y
clear
sleep 2
if [ -e $PREFIX/glibc ]; then
    echo -n "Removing previous glibc. Continue? (Y/n) "
    read i
    if [ "$i" = "Y" ] || [ "$i" = "y" ]; then
        rm -rf $PREFIX/glibc
    else
        return 1
    fi
fi

INSTALL_WOW64=0

echo "Select an option"
echo "1) Install WineBox with box86 build (only)"
echo ""
echo -n "Selected number: "
read i
case "$i" in
1)
https://github.com/Chrisisthebestfhcigcyi/WineBox/releases/tag/Glibc


